{
  "name": "nestjs-microservices-todo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:dev": "concurrently \"npm run start:dev --workspace=api-gateway\" \"npm run start:dev --workspace=user-service\" \"npm run start:dev --workspace=task-service\"",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}